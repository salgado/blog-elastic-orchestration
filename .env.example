# Elasticsearch Configuration (REQUIRED)
# Get these values from your Elastic Cloud deployment
ELASTIC_ENDPOINT=https://your-deployment.es.region.gcp.elastic-cloud.com:443
ELASTIC_API_KEY=your-base64-encoded-api-key-here

# Ollama Configuration (REQUIRED)
# Make sure you have pulled this model: ollama pull llama3.1:8b
OLLAMA_MODEL=llama3.1:8b

# Elasticsearch Indices (OPTIONAL - defaults provided)
ELASTIC_INDEX_LOGS=incident-logs
ELASTIC_INDEX_STATE=agent-checkpoints
ELASTIC_INDEX_MEMORY=agent-memory

# Reflection Pattern Configuration (OPTIONAL - defaults provided)
MAX_REFLECTION_ITERATIONS=3
QUALITY_THRESHOLD=0.8
